const expect = require('chai').expect;
// Test suite
describe('Mocha Sanity Check', () => {
    // Test spec (unit)
  it('it should run our tests using npm', () => {
    expect(true).to.be.ok;
  });
});
